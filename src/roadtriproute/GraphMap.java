/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package roadtriproute;

import java.awt.Point;

public class GraphMap {
    /*private Graph GeographicGraph;
    
    public Graph getGeographicGraph() {
        return GeographicGraph;
    }
    
    public void GraphMap(String IntersectionFile) {
        double distance;
        GeographicGraph = new Graph(200);
        for (every data on intersectionfile) {
            Point P = new Point(1,2);        
            GeographicGraph.AddVertex(P);
        }
        //masukin ke list of intersection
        //read edge
        for (every data on edgeList) {
            distance = 1.getEuclidian(2);
            GeographicGraph.AddEdge(1, 2, distance);
        }
    }*/
}
